# /etc/nginx/conf.d/marketing.conf
server {
    listen 80;
    server_name marketing.company.com;
    root /var/www/marketing;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}

# /etc/nginx/conf.d/blog.conf
server {
    listen 80;
    server_name blog.company.com;
    root /var/www/blog;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}

# /etc/nginx/conf.d/shop.conf
server {
    listen 80;
    server_name shop.company.com;
    root /var/www/shop;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}